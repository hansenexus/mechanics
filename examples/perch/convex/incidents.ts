/** Incidents: what a run of failing checks turns into. */
import { mutation, query } from "./_generated/server";

export const list = query(async () => []);
export const acknowledge = mutation(async () => null);
export const resolve = mutation(async () => null);
export const addUpdate = mutation(async () => null);
