/** Status-page subscribers, double opt-in. */
import { mutation } from "./_generated/server";

export const subscribe = mutation(async () => null);
export const confirm = mutation(async () => null);
export const unsubscribe = mutation(async () => null);
