/** The public status page's configuration. */
import { mutation, query } from "./_generated/server";

export const get = query(async () => null);
export const updateBranding = mutation(async () => null);
