/**
 * Internal debugging helper — deliberately underscore-prefixed. It is excused
 * from needing a mechanic by the `coverage.ignore` glob in
 * `mechanics/_config.yaml`, which is what that mechanism is for.
 */
export function dumpStoreIndex(): void {
  throw new Error("not implemented — replace this template with your own CLI");
}
