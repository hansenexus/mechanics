/** `status` — list snapshots and store health. Stub: this is a template. */
export async function status(opts: { json?: boolean } = {}): Promise<string> {
  void opts;
  throw new Error("not implemented — replace this template with your own CLI");
}
