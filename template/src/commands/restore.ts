/** `restore <id> <target>` — restore a snapshot. Stub: this is a template. */
export async function restore(
  id: string,
  target: string,
  opts: { clean?: boolean } = {}
): Promise<void> {
  void id;
  void target;
  void opts;
  throw new Error("not implemented — replace this template with your own CLI");
}
