/** `backup <dir>` — snapshot a source directory. Stub: this is a template. */
export async function backup(dir: string, opts: { tag?: string } = {}): Promise<string> {
  void dir;
  void opts;
  throw new Error("not implemented — replace this template with your own CLI");
}
