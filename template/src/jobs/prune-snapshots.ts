/** Scheduled prune of snapshots past the retention window. Stub. */
export async function pruneSnapshots(opts: { dryRun?: boolean } = {}): Promise<number> {
  void opts;
  throw new Error("not implemented — replace this template with your own job");
}
