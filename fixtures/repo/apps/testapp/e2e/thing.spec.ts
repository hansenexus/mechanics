// @mechanic testapp.core.view-thing
// Fixture spec — link discovery only reads the annotation above and the
// describe title below; nothing here executes in unit tests.
declare function describe(title: string, fn: () => void): void;

describe("testapp.core.view-thing — renders the list", () => {});
